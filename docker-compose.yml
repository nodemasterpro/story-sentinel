version: '3.8'

services:
  story-sentinel:
    build: .
    container_name: story-sentinel
    restart: unless-stopped
    
    # Load environment variables from .env file
    env_file:
      - .env
    
    # Persistent data volume
    volumes:
      - sentinel-data:/data
    
    # API port
    ports:
      - "8080:8080"
    
    # Health check
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8080/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
    
    # Network access to host (for connecting to Story node)
    network_mode: host
    
    # Resource limits (optional)
    deploy:
      resources:
        limits:
          memory: 512M
          cpus: '0.5'
        reservations:
          memory: 256M
          cpus: '0.25'

volumes:
  # Named volume for persistent data
  sentinel-data:
    driver: local